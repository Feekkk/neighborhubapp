import 'package:flutter/material.dart';

class AddAssign extends StatelessWidget {
  const AddAssign({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Assign Member'),
      ),
      body: const Center(
        child: Text('Assign Member Page'),
      ),
    );
  }
}